{
  "name": "@imageall/server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "test": "bun test ./src",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "bun build ./src/index.ts --outdir ./dist --format esm --target bun"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.3.3",
    "@imageall/core": "workspace:*",
    "elysia": "^1.3.8"
  }
}
